#include <bits/stdc++.h>

using namespace std;

const int kMaxN = 1e6;

int n, x, y;
int A[kMaxN], B[kMaxN];

int main() {
  cin >> n >> x >> y;
  for (int i = 1; i <= n; i++) { cin >> A[i]; }
  for (int i = 1; i <= n; i++) { cin >> B[i]; }
  for (int i = 1; i < (n << 1); i++) {
  }
  return 0;
}
