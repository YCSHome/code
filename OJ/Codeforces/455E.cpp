#include <bits/stdc++.h>

using namespace std;

const int MAXN = 1e6;

int n, m;
int a[MAXN];

int main() {
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  cin >> m;
  while (m--) {
    int l, r;
    cin >> l >> r;
  }
  return 0;
}
